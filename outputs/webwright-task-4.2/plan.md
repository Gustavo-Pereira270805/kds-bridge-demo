# Critical Points

- [ ] CP1: Initial state — `data-range="today"` button has `aria-pressed="true"`, all others have `aria-pressed="false"`
- [ ] CP2: Click "Ontem" → that button has `aria-pressed="true"`, "Hoje" and others have `aria-pressed="false"`
- [ ] CP3: Click "Últimos 7 dias" → that button has `aria-pressed="true"`, others have `aria-pressed="false"`
- [ ] CP4: Click "Últimos 30 dias" → that button has `aria-pressed="true"`, others have `aria-pressed="false"`
- [ ] CP5: Click "Hoje" again → state returns to initial (Hoje pressed)
- [ ] CP6: No JS console errors during the test
